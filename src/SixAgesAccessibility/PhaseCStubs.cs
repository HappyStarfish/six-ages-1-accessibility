// All Phase-C stubs that used to live here have been replaced with the real
// implementations ported from the LGO accessibility mod (with RLTW-specific
// adaptations where the SA2 surface differs). The file is retained empty as a
// marker so the Phase-C handoff history is still visible in git.

namespace SixAgesAccessibility
{
}
